---
title: Input
description: A styled input component for forms and user input
icon: TextCursorInput
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Input } from '@/registry/primitives/input';

<div className="my-8 max-w-sm">
  <Input placeholder="Enter your code here..." />
</div>

A form input component with consistent styling, focus states, and support for validation states.

## Installation

<Tabs items={['CLI', 'Manual']}>
  <Tab value="CLI">
    ```bash
    npx shadcn@latest add https://nteract-elements.vercel.app/r/input.json
    ```
  </Tab>
  <Tab value="Manual">
    Copy from the [nteract/elements registry](https://github.com/nteract/elements/tree/main/registry/primitives).
  </Tab>
</Tabs>

## Usage

```tsx
import { Input } from "@/registry/primitives/input"

export function Example() {
  return <Input placeholder="Enter text..." />
}
```

## Examples

### Default

<div className="my-4 max-w-sm">
  <Input placeholder="Default input" />
</div>

```tsx
<Input placeholder="Default input" />
```

### With Label

<div className="my-4 max-w-sm space-y-2">
  <label htmlFor="kernel" className="text-sm font-medium">Kernel Name</label>
  <Input id="kernel" placeholder="python3" />
</div>

```tsx
<label htmlFor="kernel" className="text-sm font-medium">
  Kernel Name
</label>
<Input id="kernel" placeholder="python3" />
```

### File Input

<div className="my-4 max-w-sm">
  <Input type="file" />
</div>

```tsx
<Input type="file" />
```

### Disabled

<div className="my-4 max-w-sm">
  <Input placeholder="Disabled input" disabled />
</div>

```tsx
<Input placeholder="Disabled input" disabled />
```

### With Default Value

<div className="my-4 max-w-sm">
  <Input defaultValue="print('Hello, world!')" />
</div>

```tsx
<Input defaultValue="print('Hello, world!')" />
```

### Different Types

<div className="my-4 max-w-sm space-y-4">
  <Input type="text" placeholder="Text input" />
  <Input type="email" placeholder="Email input" />
  <Input type="password" placeholder="Password input" />
  <Input type="number" placeholder="Number input" />
  <Input type="search" placeholder="Search..." />
</div>

```tsx
<Input type="text" placeholder="Text input" />
<Input type="email" placeholder="Email input" />
<Input type="password" placeholder="Password input" />
<Input type="number" placeholder="Number input" />
<Input type="search" placeholder="Search..." />
```

### Invalid State

The input supports `aria-invalid` for validation states:

<div className="my-4 max-w-sm">
  <Input aria-invalid="true" defaultValue="invalid input" />
</div>

```tsx
<Input aria-invalid="true" defaultValue="invalid input" />
```

## Props

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `type` | `string` | `"text"` | Input type (text, email, password, number, etc.) |
| `placeholder` | `string` | — | Placeholder text |
| `disabled` | `boolean` | `false` | Disable the input |
| `className` | `string` | — | Additional CSS classes |
| `...props` | `InputHTMLAttributes` | — | All standard input attributes |

## Accessibility

- The input has proper focus-visible styles for keyboard navigation
- Supports `aria-invalid` for validation states
- Use with `<label>` elements for proper accessibility