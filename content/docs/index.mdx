---
title: nteract elements
description: A component registry for building notebook interfaces
---

import { Card, Cards } from 'fumadocs-ui/components/card';
import { Callout } from 'fumadocs-ui/components/callout';

A collection of React components for building notebook interfaces. Designed for computational environments, Jupyter-compatible outputs, and interactive data exploration.

<Callout type="info">
  **Work in Progress** — We're bringing the nteract components out of hibernation and into the future with [shadcn](https://ui.shadcn.com). More components coming soon!
</Callout>

## Installation

### Quick Setup (Recommended)

Add nteract to your shadcn registry:

```bash
pnpm dlx shadcn@latest registry add @nteract
```

This configures your `components.json` with the @nteract registry. Then install any component:

```bash
npx shadcn@latest add @nteract/ansi-output
npx shadcn@latest add @nteract/media-router
```

### Alternative: Direct URL

Install directly without registry configuration:

```bash
npx shadcn@latest add https://nteract-elements.vercel.app/r/ansi-output.json
```

Or browse the component documentation to copy and adapt components for your project.

## Outputs

Components for rendering Jupyter notebook cell outputs.

<Cards>
  <Card 
    title="ANSI Output" 
    description="Render ANSI escape sequences as colored terminal output"
    href="/docs/outputs/ansi-output" 
  />
  <Card 
    title="Image Output" 
    description="Render images from base64 data or URLs"
    href="/docs/outputs/image-output" 
  />
  <Card 
    title="HTML Output" 
    description="Render HTML content like pandas DataFrames"
    href="/docs/outputs/html-output" 
  />
  <Card 
    title="SVG Output" 
    description="Render SVG graphics and vector images"
    href="/docs/outputs/svg-output" 
  />
  <Card 
    title="Markdown Output" 
    description="Render Markdown with GFM and syntax highlighting"
    href="/docs/outputs/markdown-output" 
  />
  <Card 
    title="JSON Output" 
    description="Render JSON in an interactive tree view"
    href="/docs/outputs/json-output" 
  />
  <Card 
    title="Media Router" 
    description="Auto-select renderer based on MIME type"
    href="/docs/outputs/media-router" 
  />
</Cards>

## Why nteract elements?

- **Notebook-first**: Built specifically for computational notebook interfaces
- **ANSI support**: Preserve colored terminal output that developers expect
- **Composable**: Mix and match components for your notebook UI
- **TypeScript**: Full type safety out of the box
- **Tailwind**: Styled with Tailwind CSS, easy to customize